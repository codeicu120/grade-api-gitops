apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-jp-api
  namespace: jpapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: new-jp-api
  template:
    metadata:
      labels:
        app: new-jp-api
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: grade-submission-api
        image: ghcr.io/codeicu120/new-jp:5812a0fad36440ea457095fd96b4bcdbefb0db0b
        ports:
        - containerPort: 8002
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: new-jp-api
  namespace: jpapi
spec:
  selector:
    app: new-jp-api
  ports:
  - port: 8002
    targetPort: 8002
  type: ClusterIP
